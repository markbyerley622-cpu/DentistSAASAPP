# SmileDesk Backend Environment Configuration

# ===========================================
# SERVER
# ===========================================
PORT=3001
NODE_ENV=development
API_URL=https://your-app.com

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# ===========================================
# DATABASE
# ===========================================
DATABASE_URL=postgresql://user:password@host:5432/smiledesk

# ===========================================
# JWT AUTHENTICATION
# ===========================================
JWT_SECRET=your-jwt-secret-key
JWT_REFRESH_SECRET=your-jwt-refresh-secret-key

# ===========================================
# ENCRYPTION
# ===========================================
# 32-character hex key for encrypting sensitive data (API keys, etc)
ENCRYPTION_KEY=your-32-character-encryption-key

# ===========================================
# CELLCAST SMS (Required for SMS functionality)
# ===========================================
# Global CellCast API key (fallback if not set per-user)
CELLCAST_API_KEY=your-cellcast-api-key

# CellCast dedicated phone number for SMS
CELLCAST_PHONE_NUMBER=+61481073412

# ===========================================
# TWILIO (Legacy - Optional)
# ===========================================
# Twilio is no longer required if using CellCast + PBX webhooks
# These are kept for backwards compatibility during migration
# TWILIO_ACCOUNT_SID=your-twilio-account-sid
# TWILIO_AUTH_TOKEN=your-twilio-auth-token

# ===========================================
# WEBHOOK CONFIGURATION
# ===========================================
# PBX Missed Call Webhook URLs:
#   Generic:     POST /api/pbx/missed-call
#   3CX:         POST /api/pbx/missed-call/3cx
#   RingCentral: POST /api/pbx/missed-call/ringcentral
#   Vonage:      POST /api/pbx/missed-call/vonage
#   FreePBX:     POST /api/pbx/missed-call/freepbx
#   8x8:         POST /api/pbx/missed-call/8x8
#   Zoom:        POST /api/pbx/missed-call/zoom
#
# CellCast Inbound SMS Webhook:
#   POST /api/sms/incoming
#
# CellCast Delivery Status Webhook:
#   POST /api/sms/status
